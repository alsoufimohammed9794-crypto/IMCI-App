name: IMCI-Android-Build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-pip build-essential git python3 python3-dev ffmpeg libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev libportmidi-dev libswscale-dev libavformat-dev libavcodec-dev zlib1g-dev libgstreamer1.0-dev

      - name: Install Buildozer
        run: pip install buildozer

      - name: Build with Buildozer
        run: |
          # هذا السطر يضمن وجود ملف الإعدادات قبل البدء
          [ -f buildozer.spec ] || buildozer init
          yes | buildozer android debug
        
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: IMCI-Package
          path: bin/*.apk
